import * as path from 'node:path';
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

const REPO_NAME = process.env.VITE_REPO_NAME || 'PL';

export default defineConfig(({ mode }
